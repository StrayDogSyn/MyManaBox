# CardForge Runtime Dependencies
# Install with: pip install -r requirements.txt

# Core
python-dotenv>=1.0.0
pydantic>=2.5.0

# Async HTTP
aiohttp>=3.9.0
aiofiles>=23.0.0
httpx>=0.25.0
requests>=2.31.0

# Data processing
pandas>=2.0.0

# Database
aiosqlite>=0.19.0

# CLI
click>=8.1.0
rich>=13.0.0

# GUI - PyQt6 (Professional Desktop GUI)
# PyQt6-Qt6 is a transitive dependency of PyQt6 and does not need to be listed separately.
PyQt6>=6.6.0

# Web server (cardforge.py 'web' command)
uvicorn>=0.27.0
fastapi>=0.109.0

# MCP (Claude Desktop integration)
mcp>=1.0.0
